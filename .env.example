# Jeffrey HQ — Environment Variables
# Copy this file to .env.local for local development

# Dashboard password (shared secret for all users)
HQ_PASSWORD=your-secure-password-here

# JWT session secret — must be at least 32 characters, random!
# Generate with: openssl rand -base64 32
SESSION_SECRET=change-me-in-production-min-32-chars!!
